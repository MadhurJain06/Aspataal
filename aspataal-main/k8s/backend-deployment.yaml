apiVersion: apps/v1
kind: Deployment
metadata:
  name: aspataal-backend
  labels:
    app: aspataal-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aspataal-backend
  template:
    metadata:
      labels:
        app: aspataal-backend
    spec:
      containers:
        - name: aspataal-backend
          image: aspataal-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: PORT
              value: "5000"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
